/**
 * Site-level identity constants — single source of truth.
 * The contact address was previously hardcoded in each privacy policy page;
 * anything that needs to reach the site owner should import it from here.
 */
export const CONTACT_EMAIL = 'info@waytosantiagoguide.com';

/** The person who writes and maintains the guide (schema `author` / `founder`). */
export const AUTHOR_NAME = 'Hugo';
